// let class = "Hello World";
//  // class is the reserved keyword in JavaScript, so it cannot be used as an identifier.  This will result in a syntax error.


// let if = true;
// // if is also the reserved keyword in JavaScript, so it cannot be used as an identifier.  This will result in a syntax error.


// let return = 5;
// // return is also the reserved keyword in JavaScript, so it cannot be used as an identifier.  This will result in a syntax error.


// let myVariable = 10;
// // This is a valid identifier and can be used without any issues

// //they look like keywords but aren't reserved
// let undefined = 5 // works but Never do this
// let Infinity = 10 // works but Never do this