class Person {
    // Attributes (Properties) → Data of the object
    name;
    email;
    salary;

    // Behaviour (Methods) → Actions the object can perform
    sleep() { }
    eat() { }
}

// CAB -> Class contains attribute, behaviour