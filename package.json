{
  "name": "learning-playwright",
  "version": "1.0.0",
  "private": true,
   "type": "module",
  "scripts": {
    "test": "npx playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.0"
  }
}
