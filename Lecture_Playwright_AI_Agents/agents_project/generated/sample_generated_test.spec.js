// spec: plans/sample_test_plan.md
// Generated by: Playwright Test Generator Agent
// Source scenario: TC-001 - Homepage Load Verification
// Date: 2026-03-19

const { test, expect } = require('@playwright/test');

test.describe('Homepage Verification', () => {
  test('Verify homepage loads with correct title', async ({ page }) => {
    // 1. Navigate to the homepage
    await page.goto('/');

    // 2. Verify the page title
    await expect(page).toHaveTitle('The Internet');

    // 3. Verify the main heading is visible
    const heading = page.locator('h1.heading');
    await expect(heading).toBeVisible();
    await expect(heading).toHaveText('Welcome to the-internet');

    console.log('✅ Agent-generated test: Homepage verification passed');
  });
});
